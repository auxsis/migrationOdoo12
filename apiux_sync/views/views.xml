<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="apiux_sync.list">
      <field name="name">apiux_sync list</field>
      <field name="model">apiux_sync.apiux_sync</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="apiux_sync.action_window">
      <field name="name">apiux_sync window</field>
      <field name="res_model">apiux_sync.apiux_sync</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="apiux_sync.action_server">
      <field name="name">apiux_sync server</field>
      <field name="model_id" ref="model_apiux_sync_apiux_sync"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="apiux_sync" id="apiux_sync.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="apiux_sync.menu_1" parent="apiux_sync.menu_root"/>
    <menuitem name="Menu 2" id="apiux_sync.menu_2" parent="apiux_sync.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="apiux_sync.menu_1_list" parent="apiux_sync.menu_1"
              action="apiux_sync.action_window"/>
    <menuitem name="Server to list" id="apiux_sync" parent="apiux_sync.menu_2"
              action="apiux_sync.action_server"/>
    -->
  </data>
</odoo>